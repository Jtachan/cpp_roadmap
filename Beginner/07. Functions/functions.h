// Next block runs if 'FUNCTIONS_H' is not defined.
#ifndef FUNCTIONS_H
    #define FUNCTIONS_H

    // Functions declarations

    /**
     * @brief Calculates the square value.
     * 
     * Given a number, calculates and returns the square value of itself.
     * 
     * @param num - Value for which the square has to be calculated.
     * @return int - Square value of the given integer. 
     */
    int square(int num);

    /**
     * @brief Greets the user.
     */
    void greet();
#endif
